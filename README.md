# akirolabs-validator

## Install
Navigate to `frontend` and `validator` directories and run `yarn`.
```
cd frontend
yarn install
cd ../validator
yarn install
```

## Run Project
Run `start.sh` file:
```
bash ./start.sh
```
